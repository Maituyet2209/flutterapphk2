import 'package:flutter/material.dart';

class bodyPersonalTaskList extends StatelessWidget {
  const bodyPersonalTaskList({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Container(

    ));
  }
}
